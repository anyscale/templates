head_node_type:
  name: head
  instance_type: m5.2xlarge

worker_node_types:
  - name: gpu-worker
    instance_type: g4dn.xlarge   # T4 16GB — one per training worker
    min_workers: 2
    max_workers: 2
