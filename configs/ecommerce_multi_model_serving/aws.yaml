head_node_type:
  name: head
  instance_type: m5.2xlarge

worker_node_types:
  - name: gpu-worker
    instance_type: g4dn.xlarge   # T4 16GB — Ray Serve replicas
    min_workers: 1
    max_workers: 4
