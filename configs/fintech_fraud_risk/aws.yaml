head_node_type:
  name: head
  instance_type: m5.4xlarge

worker_node_types:
  - name: cpu-worker
    instance_type: m5.4xlarge   # 16 vCPU, 64GB RAM — feature engineering + scoring
    min_workers: 2
    max_workers: 2
