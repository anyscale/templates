soundfile==0.13.1
# Match the base image's numpy (anyscale/ray-llm:2.57.0-py312-cu130 ships numpy 2.2.6). The test
# layers this lock on the stock image with `uv pip install --no-deps`, so a mismatched numpy
# would force-upgrade it and break the image's numpy-compiled pandas/scipy (ABI).
numpy==2.2.6
