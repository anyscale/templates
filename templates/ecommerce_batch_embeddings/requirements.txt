ray[data]>=2.9
sentence-transformers>=2.2
transformers>=4.35
torch==2.7.0
pillow>=10.0
# Pin cross-node-pickled libs (numpy/pyarrow/pandas) to the base image — Ray Data pickles them
# between head and workers, so a version mismatch breaks unpickling.
pyarrow==19.0.1
numpy==1.26.4
pandas==2.3.3
faker>=18.0
scikit-learn>=1.3
