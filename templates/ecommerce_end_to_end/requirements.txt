# ML stack — the image doesn't ship these, so pin them here.
torch==2.7.1
transformers==4.52.3
sentence-transformers==5.7.0
hf_transfer==0.1.9

# The optional Streamlit demo UI is a laptop-side client, not a cluster
# dependency — see setup/STREAMLIT_UI.md for how to run it.
