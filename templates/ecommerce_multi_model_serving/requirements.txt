ray[serve]>=2.9
fastapi==0.133.0
starlette==1.0.1
sentence-transformers>=2.2
transformers>=4.35
# Newest torch with a cu128 wheel (PyPI has higher, but CPU-only — this keeps the GPU build).
torch==2.11.0
faiss-cpu>=1.7
numpy==1.26.4
pandas>=2.0
pyarrow==19.0.1
faker>=18.0
requests
pydantic>=2.0
