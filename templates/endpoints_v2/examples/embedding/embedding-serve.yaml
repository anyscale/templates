applications:
- name: embedding-endpoint
  route_prefix: /
  import_path: aviary_private_endpoints.backend.server.run:router_application
  args:
    embedding_models:
      - "./sample_model_configs/embedding_models/thenlper--gte-large.yaml"
    models: []
