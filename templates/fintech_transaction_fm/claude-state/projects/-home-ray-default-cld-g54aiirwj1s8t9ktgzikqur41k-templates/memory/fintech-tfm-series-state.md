---
name: fintech-tfm-series-state
description: State of the fintech_transaction_fm notebook series (Parts 1-9) and known open issues
metadata: 
  node_type: memory
  type: project
  originSessionId: 27bfccd3-8165-4406-85f6-09e28afd43a0
---

`templates/templates/fintech_transaction_fm` is a 9-part notebook series (01–09), each runnable at `mini` (CPU) via papermill. The scale name was renamed `smoke`→`mini` everywhere.

**2026-07-09 (close-out) — nb01–06 COMPLETE, Zach manually reviewing before anything else.** Full mini chain 02→06 papermill-green CPU-only; nb04/06 prose passes done (fm = range 0.04–0.06 everywhere; nb06's false "fm overtakes raw at full" fixed — fusion does, fm-only doesn't, theirs doesn't either); "smoke" = zero occurrences repo-wide; NEW `scripts/run_pipeline.py` composes the five notebook stage functions with skip-guards + --force (old destructive orchestrator gone). NEXT after his review: nb08 (rewrite around new run_pipeline, verify --force mini), nb07 (needs NEW Serve app — src/serve.py serves the OLD model), nb09 (rebuild on PERFORMANCE.md stories, delete dead modules/scripts), then presentation rebuild. RESUME_HERE.md top section is the authoritative handoff.

**2026-07-09 — RAY DATA DIRECTIVE LANDED END-TO-END (PLAN_RAY_DATA.md in repo is authoritative; all committed+pushed).** Zach directive: CPU stages must scale independently of GPU stages via Ray Data. Done in stages 0-5, each with an identity check vs NVIDIA's single-GPU reference: Stage 0 CPU tokenizer mirror (`src/nvtokenize_cpu.py`) byte-identical incl. reversing cudf's hash (= murmur3 + boost hash_combine, i.e. +0x9E3779B9); Stage 1 distributed split (19,508,123 rows equal in value+order via explicit `__seq__` column — engine block order is NOT trustworthy, measured; eval sets byte-equal; `source_parquet/` seq-tagged shards = Zach's preset-parquet demo artifact); Stage 2 corpus bit-for-bit (64,335×4096); Stage 3 streaming CPU→GPU embed (lbl/raw byte-equal, embeddings kernel-precision); Stage 4 downstream re-run raw **0.1238 exact**, fusion within noise, **fm point unstable on kernel noise (0.0396 vs 0.0614, early-stop knife-edge) → fm is now quoted as RANGE 0.04–0.06 (3–5×) everywhere** (Zach delegated the call). nb01/02/03/05 rewritten around the real pipelines, all papermill-green at mini which is now genuinely CPU-only (needed a 2nd guard-only local change to vendored decoder_inference: NVIDIA's code can't run without a CUDA driver). Remaining: nb04+nb06 prose passes ("smoke" purge, fm range in nb06), nb07/08/09 still run the DEAD old pipeline (rebuild decision pending), then presentation rebuild (PRESENTATION_DRAFT.md has captured beats).

**2026-07-04 (afternoon) — clean full run in the notebooks; two fixes committed+pushed:**
(1) **nb 04 pretrain re-run** on 8×A10G was clean: 1h59m, 8 epochs, final ppl **1.662** (best yet), `model/full` refreshed. Autoscaler brought 8 GPUs up in ~2.5 min (recovered from a 4×A10G LaunchFailed → 8×1×A10G) and scaled them **down** after — good "match resources to workload" bookend. Runtimes logged to `/mnt/cluster_storage/transaction-fm/PRETRAIN_MONITOR.log` via `scripts/monitor_pretrain.py`.
(2) **Caching fix (commit 6c15f613):** skip-guards were existence-only → nb 05 reused embeddings from the OLD model after the retrain. Added `src.paths.stale_or_missing` (dir-aware mtime); nb 03 (tokenized vs raw/splits) and nb 05 (embeddings vs model+tokenized) now rebuild when upstream is newer. **Gotcha (PERFORMANCE.md §11): mtime can't detect PARTIAL writes** — killing a stage mid-run leaves a half-written dir that looks "current" → must `rm -rf` the output dir before re-running. Bit us twice with nb 05.
(3) **STRANDED-GPU ROOT CAUSE CONFIRMED + FIX (commit 9d34c640, PERFORMANCE.md §4a):** nb 05 embed pulled up **28 A10Gs, only 6 used** because `balanced_eval_sample` requests ~**584 vCPUs** and the **workspace compute config has GPU nodes only** → autoscaler grabs A10Gs for their CPUs, stranding the GPUs. FIX = add a **CPU-only worker group to the WORKSPACE compute config** (rec: `m5.8xlarge`, min 0/max 16, + `instance_ranking_strategy custom_group_order [cpu-workers, gpu-1x]`). **`job_config.yaml` already has this** (the Job path was fine; the interactive workspace diverged). Zach has full AWS access and is adding CPU nodes. Also added `SCALING_TIERS.md` (deck level-setting doc: tiers = walls not sizes; laptop→1GPU→1node→multinode→fleet; economic boundary = "must reserve capacity ahead of time").
**DONE — full end-to-end notebook run complete, BEST RESULT (commit 11368207).** Zach added the CPU node group (`m5.8xlarge`/`32cpu-128gb`) to the workspace; nb 05 re-embedded the full 3.44M (998K balanced train + 2.44M full test) with sampling on CPU nodes (no GPU stranding), then nb 06: **fusion AP 0.0690 > raw 0.0500 = +38%** (up from +24%; NVIDIA +42%). AUC ours-vs-NVIDIA: raw 0.976/0.989, fm 0.860/0.878, fusion 0.984/0.993. Absolute AP lower = eval protocol (our full 2.44M holdout vs their 100K stratified ~120 frauds), NOT modeling — the LIFT is comparable and close. Side-by-side table + all context in CHANGES.md RESUME-HERE (2026-07-04 afternoon). Remaining polish: notebook prose pass; partial-write-proof cache guard (`_SUCCESS` marker); optional distributed-XGB 512-dim scale-up.

**2026-07-04 (late) — APPLES-TO-APPLES vs NVIDIA investigation (CHANGES.md RESUME-HERE is authoritative).** Zach demanded a true apples-to-apples with NVIDIA's PUBLISHED numbers (raw 0.1238/0.1424, fm 0.0123, fusion 0.1755) — do NOT re-derive their side, only compute OURS on their exact protocol. RESEARCHED their actual notebooks (fetched from repo, not our reconstruction): temporal 80/10/10 (same as us), 13 features (only string cols OrdinalEncoded), 1M balanced train, 100K stratified test (~120 frauds), per-set HPO + early-stop on val, single-txn embed + PCA 512→64. FINDINGS: (1) **our FM beats theirs on fm-only** (per-set HPO: fm AUC ~0.95 vs 0.878, AP ~0.044–0.072 vs 0.0123) — cleanest & favorable. (2) raw/fusion single-number at ~120 frauds is UNRELIABLE (faithful run: raw AP 0.29 best_iter=0, raw>fusion = artifact). (3) **draw variance huge** — bootstrap 20×100K → fusion AP 0.051–0.129 (2.5×); NVIDIA's is one noisy draw, but even our max shared-recipe draw < 0.176 so not purely luck. (4) stable view = full-holdout AP (fusion 0.064–0.069, +36–38% lift). (5) recipe-sensitive: fm AP 0.005 (shared) vs 0.044–0.072 (per-set HPO). **2026-07-05 (~3am) — FINAL: RAN NVIDIA'S LITERAL CODE. Components reproduce, +42% fusion lift does NOT (see [`FINDINGS_FM_REPRODUCTION.md`](templates/templates/fintech_transaction_fm/FINDINGS_FM_REPRODUCTION.md), authoritative, commit 50e6ba5e).** Cloned NVIDIA's repo + downloaded their real pretrained weights (git-LFS via GitHub media URL, 58MB), ran their literal NB01→NB04→NB05 (their tokenizer w/ temporal_encoding+category_hierarchy, their model, their embed, their fusion, their eval) in Anyscale on a GPU worker — ONLY change: cuml KBinsDiscretizer→sklearn shim (identical math). RESULT: raw AP 0.137≈their 0.124 ✅, fm 0.012≈their 0.012 ✅, **fusion 0.042 vs their 0.176 ❌ and BELOW raw** (overfit signature — impossible for correct fit). So their +42% fusion lift does NOT reproduce from their own code on a fresh draw. NOT fraud (components reproduce) — a reproducibility failure of the unstable fusion recipe. **DID NOT run the confirming bootstrap — that's step 1 next: bootstrap their fusion over 20–50 draws to prove it's consistently ≠0.176 BEFORE any external/VP claim.** Zach stopped for the night, exhausted; taking to management. Env note: cudf install bumped workspace numpy→2.4 (conflicts scipy/matplotlib — may need numpy<2 pin). Artifacts (ephemeral scratchpad): nvidia_tfm/ repo+weights, /mnt/cluster_storage/nvidia_{model,data}, nv_embed_fuse.py. **SESSION META (huge, brutal): I overclaimed in every direction for hours (fusion beats raw→invalidated→we beat them→they faked) and had to retract each — every "we out/underperform" was OUR bug until, finally, running THEIR literal code isolated it to their fusion. HOLD THE FIDELITY PRINCIPLE, never accuse the source without running their code, verify before concluding. See [[fintech-tfm-fidelity-principle]] [[fintech-tfm-working-style]].**

**(prior, superseded) 2026-07-04 (very late) — NVIDIA REPRODUCTION INVESTIGATION (see FINDINGS_FM_REPRODUCTION.md, earlier version, commit 48b2cb03).** After a very long, error-prone session: **components reproduce, fusion lift does NOT (in our reimpl).** raw 0.11–0.12 ≈ NVIDIA 0.124 ✅; fm 0.011 ≈ their 0.012 ✅; fusion adds +0.00 lift (bulletproof AP-max blend picks pure raw; fusion beats raw in 0–2% of 50 bootstrapped draws) vs their +42% ❌. KEY ERRORS FOUND & FIXED (ours): (1) downstream had early-stopping removed → overfit → raw collapsed to 0.05 (A/B: fixed-trees 0.049 vs early-stop 0.206); (2) **our raw was inflated ~2× by account-ID leakage** — User/Card/Merchant features memorize fraud-prone accounts, 92% test-cards-seen-in-train; drop them → raw 0.116 ≈ NVIDIA 0.124 (so earlier "we beat NVIDIA on raw" was leakage, NOT real); (3) embedding was single-txn (max_ctx=14, redundant w/ raw) — history embed (max_ctx=128, nb05 `embed.max_ctx`) matches their fm 0.011 & is complementary (corr 0.36→0.13). **NVIDIA VINDICATED on components — no fabrication; I repeatedly overclaimed "they faked/understated" and had to retract each time — DO NOT make that claim.** OPEN / definitive test NOT done: **run NVIDIA's LITERAL NB05 cuDF fusion code** — distinguishes "our fusion-reimpl bug (likely raw×emb interactions)" vs "their +42% doesn't reproduce". No claim about NVIDIA should be published before that. Zach taking findings to management. Caveats: eval noisy (~120 frauds/100K protocol), all our reimpl not their code. **Working-style lesson this session: I overclaimed in BOTH directions repeatedly (fusion beats raw → invalidated → we beat them → they faked) — every "we underperform/outperform" was OUR bug; hold the fidelity principle HARD, verify before concluding, never accuse the published source. See [[fintech-tfm-fidelity-principle]].**

ROOT CAUSE (earlier in session, superseded by above but kept for context) — PERFORMANCE.md §13, CHANGES.md RESUME-HERE: the weak raw/fusion is the DOWNSTREAM, not the FM or features. Features faithful (card_id=User*100+Card, merchant_id=Merchant Name; carried→source swap = same 0.048). A/B on identical features: fixed-trees-no-early-stop → raw 0.049; early-stop on natural val → raw **0.206** (best_iter=2). Bug: our downstream REMOVED early stopping → overfits the fraud-enriched train → collapses on natural test. raw fix confirmed 0.049→0.17–0.21 (beats NVIDIA 0.124); fm ~0.045 (beats 0.012); fusion best 0.147 (still <0.176) AND fit is UNSTABLE (raw swung 0.169→0.088 on early-stop patience alone, 5 vs 42 trees) because enriched-train/natural-test mismatch + early-stop on AUC ≠ test-AP. FIXES (target cause, NOT blind param search): (1) early-stop on AP not AUC; (2) cut enrichment mismatch (scale_pos_weight vs resample); (3) proper natural-rate val period; (4) or fixed small tree count ~5. All go in src/downstream.py. Expect fusion >0.176 once stabilized. **PROCESS LESSONS (Zach frustrated, see [[fintech-tfm-working-style]]):** don't spin up GPUs carelessly / fire-and-forget (ballooned to 28–45 then 19 GPUs twice — the embed's CPU-heavy seq-4096 read pulls A10G nodes for their CPUs; PERFORMANCE.md §12); RESEARCH how NVIDIA does it, don't guess; reuse cached embeddings (scoring is CPU-only XGBoost, GPU only for the embed forward pass); watch every compute job and confirm GPU:0 for CPU work.

**2026-07-03 — big re-architecture to NVIDIA's transaction-FM blueprint (committed, branch `zgarner_transaction_foundation_model`):** flat tokenizer (12 tokens/txn, shared vocab 6259) + Llama causal decoder (29M params: hidden 512, 8 layers, GQA 8/2, SwiGLU 1408, rope 5e5) + next-token pretrain + last-token embedding. Replaced the old field-split/MLM design. See CHANGES.md (authoritative RESUME-HERE at top). Goal: beat NVIDIA fusion **PR-AUC 0.1755** on real IBM TabFormer (24.4M txns, 0.12% fraud); old arch best was 0.0301. Metric = PR-AUC (AP) at ~0.1% prevalence.

**GPU problem SOLVED (2026-07-03):** last session was stuck on T4s (A10G capacity-failing, ~16h/epoch). After Zach rebooted, the workspace launches **A10G (23.7GB)** cleanly, up to 8 for pretrain. Uncommitted fixes on branch: `job_config.yaml` (g4dn/T4→g5/A10G), `requirements.txt` (+transformers/accelerate).

**IMPORTANT — `/mnt/cluster_storage` PERSISTS across cluster restarts** (workspace-scoped, NOT ephemeral as CHANGES.md feared). On resume, `source/` CSV, `raw/full`, `tokenized/full` (6.7G, 64,561 pretrain seqs, ~5M eval) all survived → skip nb 01/02/03, run only 04→06. Memory levers present: bf16 autocast (`pretrain.py`), `gradient_checkpointing_enable()` (`model.py`), expandable_segments.

**Ray perf/scale storylines** — a core PURPOSE of this series is to SHOW Ray addressing performance/scale issues, so capture real ones as they arise. `PERFORMANCE.md` (new, 2026-07-03) catalogs them (symptom→cause→fix→what-Ray-did→which-notebook), source material esp. for nb 09 "scaling up". Captured so far: T4-can't-fit-seq4096 (→A10G+bf16+grad-ckpt), Ray Train worker-startup-timeout auto-retry, embed fp32 OOM (→bf16+batch 256→64), **stranded GPUs (24 provisioned / 16 used = 67% — the "match resources to workload" story)**, open item: per-GPU SM saturation of the 16 busy GPUs (batch 64 maybe too small; test 128; needs nvidia-smi measurement), Ray Data CPU→GPU streaming backpressure (a win), embed throughput-bound (linear scale-out), driver-OOM-on-scoring + CPU-path autoscaler-assertion (both already fixed).

**2026-07-03 evening — first full run DONE, 2 bugs found (see CHANGES.md RESUME-HERE, authoritative):** Llama-arch full results came back WORSE than run 2: raw AP 0.017 (NVIDIA 0.124), fm 0.0023 (NV 0.0123), fusion 0.021 (NV 0.176). Two independent bugs (Zach confirmed: one raw/XGBoost, one TFM; doing raw first). **BUG 1 (raw) — real cause was scale_pos_weight, not train size (see [`NVIDIA_BASELINE.md`](templates/templates/fintech_transaction_fm/NVIDIA_BASELINE.md), authoritative).** First hypothesis (train starved to 321K) was WRONG. Added `train_keep=1.0` (train on full set) but raw got WORSE — because `scale_pos_weight=neg/pos`≈700 at natural 0.1% prevalence collapses PR-AUC (sample sweep: neg/pos→AP 0.004, sqrt(26)→0.053, fixed 12→0.051). Patched to `sqrt(neg/pos)` (interim). raw AP 0.017→~0.05, still ~2.4x below NVIDIA 0.124 AND on a different eval. Pulled NVIDIA's ACTUAL recipe (github NVIDIA-AI-Blueprints/transaction-foundation-model, NB01/05): OrdinalEncoder (we crc32-hash), balanced 1M sample + spw=1.0 (we don't), per-feature-set HPO params (we share one), 100k stratified eval (we score full 2.44M holdout → not comparable), and they use 'Merchant Name' string (our loader dropped it, kept merchant_id int). Original CSV on disk at source/card_transaction.v1.csv → exact replication possible. **NEXT: reproduce NVIDIA raw 0.124 exactly from CSV (credibility check), then decide template stance (adopt their protocol vs keep our full-pop/shared-recipe/Ray design + report both), fix hashing→ordinal, then bug #2.** **BUG 2 (TFM) — FIXED 2026-07-03 eve:** root cause was EMBEDDING CONTEXT LENGTH. NVIDIA (NB04) pretrains at 4096 but embeds each txn from only its last MAX_LENGTH=128 tokens (~10 txns); we embedded from up to 314 txns (seq_len 4096) → last-token vector washed out. Fix: `EmbeddingExtractor(max_ctx=128)` in src/embed.py (+ `--embed-max-len` in nv_downstream.py). fm AUC 0.62→0.795, fm AP 0.0023→0.0092, fusion 0.146 > raw 0.131 (FM adds lift, like NVIDIA). High cosine (0.97) was benign anisotropy, NOT collapse (red herring). Ruled out: pooling position, undertraining (16k steps, ppl 1.7), weight-load (75/75 keys), tokenizer (== NVIDIA default). Committed+pushed. Full 100k run pending. TODO: make clean (tokenize eval at 128 OR keep truncation; wire to configs/notebooks). See [[fintech-tfm-fidelity-principle]] and NVIDIA_BASELINE.md.
**2026-07-04: RECIPE BAKED INTO THE PIPELINE + NOTEBOOKS; clean full run = fusion>raw.** The validated recipe is now in src/ (not just the nv_downstream harness) and notebooks 05/06 call it (papermill-clean end-to-end at mini). Clean FULL result: raw AUC 0.974/AP 0.0466, fm 0.837/0.0063, **fusion 0.984/AP 0.0575 = +0.011 AP (+24%) over raw** (NVIDIA +42%). Changes: single-txn embed (`embed.max_ctx=14`), balanced sample before embed (`balanced_eval_sample`), real-raw features (full merchant_id + city/state/chip strings, OrdinalEncoded downstream, not hashed), downstream rewritten (balanced+spw=1, PCA-64, ONE shared fixed-rounds recipe NO early-stop), empty-group pretrain Arrow crash fixed (guarantee ≥1 window/card). Pre-staged full artifacts (tokenized/full real-raw, model/full valid=SKIP nb04, embeddings/full single-txn, downstream/full). User's run: nb03→skip04→nb05→nb06→fusion>raw. Polish left: absolute AP conservative (raw 0.047 vs NVIDIA 0.124 — full-holdout vs their 100k eval; lift is the comparable # and is +), notebook prose pass, optional distributed-XGB for 512-dim scale-up story. See CHANGES.md RESUME-HERE + NVIDIA_BASELINE.md.
**(prior) RESOLVED (2026-07-03 latest): FUSION BEATS RAW (+32%); the FM works.** The scary "fusion < raw" was a DOWNSTREAM XGBOOST fit artifact (raw & fusion used different per-set HPO params + early-stopping degenerated to ~1-tree models on enriched-train/natural-test → raw AP bounced 0.05↔0.23). Fair test (`--shared`: one identical low-lr recipe, FIXED 400 trees, no early stop) on full holdout (2718 frauds): raw AP 0.0502, fm 0.0063, fusion AP 0.0663 → fusion−raw = +0.016 AP (+32%), same direction/order as NVIDIA (+42%). FM adds real lift. Absolute AP lower than NVIDIA (conservative recipe + full-holdout vs their 100k eval) — the LIFT is the comparable quantity, positive. Lesson: skewed fraud + enriched train → early-stopping unstable; use fixed low-lr many-tree + SAME recipe across feature sets. Full FM fixes this session: single-txn embed (`--embed-max-len 14`) + PCA-64 (`--pca-dim 64`) + shared recipe. Remaining polish: raise absolute AP, distributed XGB for 512-dim scale-up (OOM'd single worker), bake fixes into notebooks/configs.
**(superseded) FM plumbing now FAITHFUL, but fusion still doesn't beat raw (2026-07-03 late):** Two more NVIDIA divergences fixed: (a) embed SINGLE txn not history (NB04 encode; `--embed-max-len 14`), (b) PCA 512→64 before XGBoost (NB05). STABLE full-holdout (2718 frauds), PCA-64: raw AUC 0.986/AP 0.196, fm 0.944/0.027, fusion 0.986/AP 0.094 (fits healthy, best_iter 10). **fusion (0.094) < raw (0.196) — FM adds NO lift**, unlike NVIDIA (fusion 0.176>raw 0.124). Hypothesis: our single-txn embedding is REDUNDANT with raw (same 12 fields); NVIDIA's fm-alone weaker (0.012) but COMPLEMENTARY → their shipped foundation model learned richer per-txn structure; our pretrain ppl 1.7 = near-trivial/weak. **Remaining gap = PRETRAIN QUALITY, not plumbing.** Testing 512-dim (no-PCA "scale up") variant. `scripts/nv_downstream.py` params: `--embed-max-len 14 --pca-dim {64|0} --tag holdout --skip-embed`. Cached full-holdout embeddings at nv_downstream/holdout_embeddings. NOTE: my earlier "we beat NVIDIA on fm-only" was WRONG (we used 512-dim vs their 64-dim PCA + noisy eval) — Zach caught it. Don't overclaim.
**(old) BUG 2 (TFM, NOT fixed):** fm-only AP 0.0023, AUC 0.634 (~random) — Llama last-token embedding not discriminative; investigate after raw. **Also fixed:** latent tokenizer crash — `emit="pretrain"` on cards w/ no train txns → empty group → ArrowConversionError (empty object/tensor cols); `emit="eval"`/`"both"` are safe (fallback guarantees ≥1 row/card). Partial fix committed (`<U16` strings) but empty *tensor* cols still crash Write after drop_columns → **run_pipeline.py/02_tokenize emit="pretrain" still broken at full — TODO fix** (revert pretrain gen to emit="both" streaming, or make empty blocks Arrow-safe). **Head node OOM-crashed once on CPU work — keep ALL heavy work distributed on workers, nothing big on driver/head.** **Now running:** eval-only re-tokenize at full train_keep=1.0 (`/tmp/retok_eval.py`, log `/mnt/cluster_storage/retok_eval.log`) — crash-safe emit="eval", ~24.4M windows, model/full kept (skip pretrain). Next: `scripts/probe_raw.py --scale full --num-workers N --use-gpu` (distributed raw-only XGBoost off tokenized eval, no embed) to measure raw AP vs 0.124. If good → re-embed (24.4M, ~2h) → downstream.

**(older) In progress (2026-07-03 ~11:53):** first full run of new arch — headless chain `scripts/03_pretrain.py→04_extract_embeddings.py→05_train_downstream.py --scale full` on 8×A10G, launched from scratchpad `run_full_tail.sh`. ~2-4h pretrain + embed + downstream. Then read 06 fusion PR-AUC vs 0.1755. Stage scripts read persisted disk artifacts and DON'T wipe (only `run_pipeline.py` wipes).

Parts: 1 Setup, 2 Load/explore, 3 Tokenize, 4 Pretrain, 5 Embed, 6 Downstream fraud, 7 Serving, 8 Run-on-Anyscale (fused `run_pipeline.py` + Job), 9 Scaling-up (4 bottlenecks: data>node, GPU-starved-by-CPU, stage-boundary I/O, serve latency).

`src/` additions made for the notebooks: `generate_data.generate_dataset_distributed`/`generate_cards` (distributed synthetic gen), `pretrain.save_checkpoint`, `tokenizer.decode_static_fields`/`decode_dynamic_window`, `embed` actor pool fix (`num_cpus=1` + `min_size=1,max_size=N` — the CPU-path inf-bundle autoscaler assertion). `scripts/generate_big.py` added.

Open issues to resolve in review (as of 2026-06-25):
- **CI (`tests/fintech_transaction_fm/tests.sh`) still runs `README.ipynb`** (monolith), not the series — and README sets `SCALE="small"` (GPU) while its prose says it runs at `mini`. Decide whether to point CI at the series.
- **Part 8 `run_pipeline.py` is destructive** (wipes all stage outputs, streams tokenized through memory so no tokenized Parquet remains). Part 9 has a guard that regenerates tokenized if missing. Decide if that UX is right or reorder/soften.
- **Bottleneck 2 (GPU idle) has no inline measurement** (no GPU at mini) — config + arithmetic + referred-out Job only.
- **Distributed generator is slow** (~150s/10k cards; per-card Python loop) — fine for demo, could vectorize.

Verified green via papermill this session: Parts 3,4,5,6,7,9. Part 8 verified via headless `run_pipeline.py` (its only executable cell). See [[fintech-tfm-working-style]].
