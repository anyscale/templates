---
name: fintech-tfm-series-state
description: State of the fintech_transaction_fm notebook series (Parts 1-9) and known open issues
metadata: 
  node_type: memory
  type: project
  originSessionId: 27bfccd3-8165-4406-85f6-09e28afd43a0
---

`templates/templates/fintech_transaction_fm` is a 9-part notebook series (01–09), each runnable at `mini` (CPU) via papermill. The scale name was renamed `smoke`→`mini` everywhere.

**2026-07-03 — big re-architecture to NVIDIA's transaction-FM blueprint (committed, branch `zgarner_transaction_foundation_model`):** flat tokenizer (12 tokens/txn, shared vocab 6259) + Llama causal decoder (29M params: hidden 512, 8 layers, GQA 8/2, SwiGLU 1408, rope 5e5) + next-token pretrain + last-token embedding. Replaced the old field-split/MLM design. See CHANGES.md (authoritative RESUME-HERE at top). Goal: beat NVIDIA fusion **PR-AUC 0.1755** on real IBM TabFormer (24.4M txns, 0.12% fraud); old arch best was 0.0301. Metric = PR-AUC (AP) at ~0.1% prevalence.

**GPU problem SOLVED (2026-07-03):** last session was stuck on T4s (A10G capacity-failing, ~16h/epoch). After Zach rebooted, the workspace launches **A10G (23.7GB)** cleanly, up to 8 for pretrain. Uncommitted fixes on branch: `job_config.yaml` (g4dn/T4→g5/A10G), `requirements.txt` (+transformers/accelerate).

**IMPORTANT — `/mnt/cluster_storage` PERSISTS across cluster restarts** (workspace-scoped, NOT ephemeral as CHANGES.md feared). On resume, `source/` CSV, `raw/full`, `tokenized/full` (6.7G, 64,561 pretrain seqs, ~5M eval) all survived → skip nb 01/02/03, run only 04→06. Memory levers present: bf16 autocast (`pretrain.py`), `gradient_checkpointing_enable()` (`model.py`), expandable_segments.

**Ray perf/scale storylines** — a core PURPOSE of this series is to SHOW Ray addressing performance/scale issues, so capture real ones as they arise. `PERFORMANCE.md` (new, 2026-07-03) catalogs them (symptom→cause→fix→what-Ray-did→which-notebook), source material esp. for nb 09 "scaling up". Captured so far: T4-can't-fit-seq4096 (→A10G+bf16+grad-ckpt), Ray Train worker-startup-timeout auto-retry, embed fp32 OOM (→bf16+batch 256→64), **stranded GPUs (24 provisioned / 16 used = 67% — the "match resources to workload" story)**, open item: per-GPU SM saturation of the 16 busy GPUs (batch 64 maybe too small; test 128; needs nvidia-smi measurement), Ray Data CPU→GPU streaming backpressure (a win), embed throughput-bound (linear scale-out), driver-OOM-on-scoring + CPU-path autoscaler-assertion (both already fixed).

**2026-07-03 evening — first full run DONE, 2 bugs found (see CHANGES.md RESUME-HERE, authoritative):** Llama-arch full results came back WORSE than run 2: raw AP 0.017 (NVIDIA 0.124), fm 0.0023 (NV 0.0123), fusion 0.021 (NV 0.176). Two independent bugs (Zach confirmed: one raw/XGBoost, one TFM; doing raw first). **BUG 1 (raw) — real cause was scale_pos_weight, not train size (see [`NVIDIA_BASELINE.md`](templates/templates/fintech_transaction_fm/NVIDIA_BASELINE.md), authoritative).** First hypothesis (train starved to 321K) was WRONG. Added `train_keep=1.0` (train on full set) but raw got WORSE — because `scale_pos_weight=neg/pos`≈700 at natural 0.1% prevalence collapses PR-AUC (sample sweep: neg/pos→AP 0.004, sqrt(26)→0.053, fixed 12→0.051). Patched to `sqrt(neg/pos)` (interim). raw AP 0.017→~0.05, still ~2.4x below NVIDIA 0.124 AND on a different eval. Pulled NVIDIA's ACTUAL recipe (github NVIDIA-AI-Blueprints/transaction-foundation-model, NB01/05): OrdinalEncoder (we crc32-hash), balanced 1M sample + spw=1.0 (we don't), per-feature-set HPO params (we share one), 100k stratified eval (we score full 2.44M holdout → not comparable), and they use 'Merchant Name' string (our loader dropped it, kept merchant_id int). Original CSV on disk at source/card_transaction.v1.csv → exact replication possible. **NEXT: reproduce NVIDIA raw 0.124 exactly from CSV (credibility check), then decide template stance (adopt their protocol vs keep our full-pop/shared-recipe/Ray design + report both), fix hashing→ordinal, then bug #2.** **BUG 2 (TFM) — FIXED 2026-07-03 eve:** root cause was EMBEDDING CONTEXT LENGTH. NVIDIA (NB04) pretrains at 4096 but embeds each txn from only its last MAX_LENGTH=128 tokens (~10 txns); we embedded from up to 314 txns (seq_len 4096) → last-token vector washed out. Fix: `EmbeddingExtractor(max_ctx=128)` in src/embed.py (+ `--embed-max-len` in nv_downstream.py). fm AUC 0.62→0.795, fm AP 0.0023→0.0092, fusion 0.146 > raw 0.131 (FM adds lift, like NVIDIA). High cosine (0.97) was benign anisotropy, NOT collapse (red herring). Ruled out: pooling position, undertraining (16k steps, ppl 1.7), weight-load (75/75 keys), tokenizer (== NVIDIA default). Committed+pushed. Full 100k run pending. TODO: make clean (tokenize eval at 128 OR keep truncation; wire to configs/notebooks). See [[fintech-tfm-fidelity-principle]] and NVIDIA_BASELINE.md.
**2026-07-04: RECIPE BAKED INTO THE PIPELINE + NOTEBOOKS; clean full run = fusion>raw.** The validated recipe is now in src/ (not just the nv_downstream harness) and notebooks 05/06 call it (papermill-clean end-to-end at mini). Clean FULL result: raw AUC 0.974/AP 0.0466, fm 0.837/0.0063, **fusion 0.984/AP 0.0575 = +0.011 AP (+24%) over raw** (NVIDIA +42%). Changes: single-txn embed (`embed.max_ctx=14`), balanced sample before embed (`balanced_eval_sample`), real-raw features (full merchant_id + city/state/chip strings, OrdinalEncoded downstream, not hashed), downstream rewritten (balanced+spw=1, PCA-64, ONE shared fixed-rounds recipe NO early-stop), empty-group pretrain Arrow crash fixed (guarantee ≥1 window/card). Pre-staged full artifacts (tokenized/full real-raw, model/full valid=SKIP nb04, embeddings/full single-txn, downstream/full). User's run: nb03→skip04→nb05→nb06→fusion>raw. Polish left: absolute AP conservative (raw 0.047 vs NVIDIA 0.124 — full-holdout vs their 100k eval; lift is the comparable # and is +), notebook prose pass, optional distributed-XGB for 512-dim scale-up story. See CHANGES.md RESUME-HERE + NVIDIA_BASELINE.md.
**(prior) RESOLVED (2026-07-03 latest): FUSION BEATS RAW (+32%); the FM works.** The scary "fusion < raw" was a DOWNSTREAM XGBOOST fit artifact (raw & fusion used different per-set HPO params + early-stopping degenerated to ~1-tree models on enriched-train/natural-test → raw AP bounced 0.05↔0.23). Fair test (`--shared`: one identical low-lr recipe, FIXED 400 trees, no early stop) on full holdout (2718 frauds): raw AP 0.0502, fm 0.0063, fusion AP 0.0663 → fusion−raw = +0.016 AP (+32%), same direction/order as NVIDIA (+42%). FM adds real lift. Absolute AP lower than NVIDIA (conservative recipe + full-holdout vs their 100k eval) — the LIFT is the comparable quantity, positive. Lesson: skewed fraud + enriched train → early-stopping unstable; use fixed low-lr many-tree + SAME recipe across feature sets. Full FM fixes this session: single-txn embed (`--embed-max-len 14`) + PCA-64 (`--pca-dim 64`) + shared recipe. Remaining polish: raise absolute AP, distributed XGB for 512-dim scale-up (OOM'd single worker), bake fixes into notebooks/configs.
**(superseded) FM plumbing now FAITHFUL, but fusion still doesn't beat raw (2026-07-03 late):** Two more NVIDIA divergences fixed: (a) embed SINGLE txn not history (NB04 encode; `--embed-max-len 14`), (b) PCA 512→64 before XGBoost (NB05). STABLE full-holdout (2718 frauds), PCA-64: raw AUC 0.986/AP 0.196, fm 0.944/0.027, fusion 0.986/AP 0.094 (fits healthy, best_iter 10). **fusion (0.094) < raw (0.196) — FM adds NO lift**, unlike NVIDIA (fusion 0.176>raw 0.124). Hypothesis: our single-txn embedding is REDUNDANT with raw (same 12 fields); NVIDIA's fm-alone weaker (0.012) but COMPLEMENTARY → their shipped foundation model learned richer per-txn structure; our pretrain ppl 1.7 = near-trivial/weak. **Remaining gap = PRETRAIN QUALITY, not plumbing.** Testing 512-dim (no-PCA "scale up") variant. `scripts/nv_downstream.py` params: `--embed-max-len 14 --pca-dim {64|0} --tag holdout --skip-embed`. Cached full-holdout embeddings at nv_downstream/holdout_embeddings. NOTE: my earlier "we beat NVIDIA on fm-only" was WRONG (we used 512-dim vs their 64-dim PCA + noisy eval) — Zach caught it. Don't overclaim.
**(old) BUG 2 (TFM, NOT fixed):** fm-only AP 0.0023, AUC 0.634 (~random) — Llama last-token embedding not discriminative; investigate after raw. **Also fixed:** latent tokenizer crash — `emit="pretrain"` on cards w/ no train txns → empty group → ArrowConversionError (empty object/tensor cols); `emit="eval"`/`"both"` are safe (fallback guarantees ≥1 row/card). Partial fix committed (`<U16` strings) but empty *tensor* cols still crash Write after drop_columns → **run_pipeline.py/02_tokenize emit="pretrain" still broken at full — TODO fix** (revert pretrain gen to emit="both" streaming, or make empty blocks Arrow-safe). **Head node OOM-crashed once on CPU work — keep ALL heavy work distributed on workers, nothing big on driver/head.** **Now running:** eval-only re-tokenize at full train_keep=1.0 (`/tmp/retok_eval.py`, log `/mnt/cluster_storage/retok_eval.log`) — crash-safe emit="eval", ~24.4M windows, model/full kept (skip pretrain). Next: `scripts/probe_raw.py --scale full --num-workers N --use-gpu` (distributed raw-only XGBoost off tokenized eval, no embed) to measure raw AP vs 0.124. If good → re-embed (24.4M, ~2h) → downstream.

**(older) In progress (2026-07-03 ~11:53):** first full run of new arch — headless chain `scripts/03_pretrain.py→04_extract_embeddings.py→05_train_downstream.py --scale full` on 8×A10G, launched from scratchpad `run_full_tail.sh`. ~2-4h pretrain + embed + downstream. Then read 06 fusion PR-AUC vs 0.1755. Stage scripts read persisted disk artifacts and DON'T wipe (only `run_pipeline.py` wipes).

Parts: 1 Setup, 2 Load/explore, 3 Tokenize, 4 Pretrain, 5 Embed, 6 Downstream fraud, 7 Serving, 8 Run-on-Anyscale (fused `run_pipeline.py` + Job), 9 Scaling-up (4 bottlenecks: data>node, GPU-starved-by-CPU, stage-boundary I/O, serve latency).

`src/` additions made for the notebooks: `generate_data.generate_dataset_distributed`/`generate_cards` (distributed synthetic gen), `pretrain.save_checkpoint`, `tokenizer.decode_static_fields`/`decode_dynamic_window`, `embed` actor pool fix (`num_cpus=1` + `min_size=1,max_size=N` — the CPU-path inf-bundle autoscaler assertion). `scripts/generate_big.py` added.

Open issues to resolve in review (as of 2026-06-25):
- **CI (`tests/fintech_transaction_fm/tests.sh`) still runs `README.ipynb`** (monolith), not the series — and README sets `SCALE="small"` (GPU) while its prose says it runs at `mini`. Decide whether to point CI at the series.
- **Part 8 `run_pipeline.py` is destructive** (wipes all stage outputs, streams tokenized through memory so no tokenized Parquet remains). Part 9 has a guard that regenerates tokenized if missing. Decide if that UX is right or reorder/soften.
- **Bottleneck 2 (GPU idle) has no inline measurement** (no GPU at mini) — config + arithmetic + referred-out Job only.
- **Distributed generator is slow** (~150s/10k cards; per-card Python loop) — fine for demo, could vectorize.

Verified green via papermill this session: Parts 3,4,5,6,7,9. Part 8 verified via headless `run_pipeline.py` (its only executable cell). See [[fintech-tfm-working-style]].
