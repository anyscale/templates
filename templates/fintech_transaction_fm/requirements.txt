ray[data,train,serve]>=2.9
torch>=2.4  # torch.amp.GradScaler("cuda") / unified autocast API in pretrain
numba>=0.61  # JIT'd hash partitioning for the Ray Data groupby shuffles
xgboost==3.2.0  # NVIDIA pin (see clean branch)
scikit-learn>=1.3
numpy>=1.24
pandas>=2.0
pyarrow>=14.0
tensorboard>=2.15  # per-step training curves (src/pretrain.py); view with tensorboard --logdir
