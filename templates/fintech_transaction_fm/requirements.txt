ray[data,train,serve]>=2.9
torch>=2.1
transformers>=4.40  # Llama causal decoder (LlamaConfig/LlamaForCausalLM) in src/model.py
accelerate>=0.30  # required by transformers Trainer/model init on the Ray Train workers
numba>=0.61  # JIT'd hash partitioning for the Ray Data groupby shuffles
xgboost==3.2.0  # PINNED to NVIDIA's version: 3.3 changed early-stopping and halves the
                # downstream AP on this fraud-rate/recipe (see FINDINGS_FM_REPRODUCTION.md)
scikit-learn==1.7.2  # NVIDIA's pin
matplotlib>=3.7  # data-exploration plots in the notebook series
seaborn>=0.13  # cleaner default styling for the exploration plots
numpy>=1.24
pandas>=2.0
pyarrow>=14.0
