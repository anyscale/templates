# datasets is layered onto the anyscale/ray-llm:2.56.0 (numpy-2 stack) base image
# via `uv pip install --no-deps`, so we keep the delta purely additive. 3.6.0 is
# the newest datasets that resolves against the base image's pandas/pyarrow
# without pulling in a pandas-3 requirement, so we pin it alongside the base
# image's numpy/pandas/pyarrow so they reinstall as no-ops (no ABI break, Ray-safe).
datasets==3.6.0
numpy==2.2.6
pandas==2.3.3
pyarrow==23.0.1
# datasets only requires huggingface-hub>=0.24.0, but unsafe-best-match resolves
# to a newer version than the image ships. Pin to the base-image version so it
# reinstalls as a no-op (base image ships transformers 5.8.0 which pairs with
# huggingface-hub 1.x).
huggingface-hub==1.13.0
