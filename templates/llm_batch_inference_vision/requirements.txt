# datasets 4.x requires the numpy-2 stack (numpy>=2, pyarrow>=21, pandas 3), which
# the base image doesn't have — it ships numpy 1.26.4 + pyarrow 19.0.1 + scipy
# compiled against numpy 1.x. 3.6.0 is the newest datasets that resolves against it.
datasets==3.6.0
