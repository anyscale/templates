# The test layers this lock on the stock anyscale/ray-llm:2.56.0-py312-cu130
# image with `uv pip install --no-deps` and propagates it to workers via Ray's
# runtime_env. Without a base-image constraint, `uv pip compile` resolves to
# whatever satisfies our requirements (e.g. huggingface-hub==0.36.2, numpy 1.x),
# which silently downgrades the image's numpy-2 / transformers-5 stack and
# breaks imports like `is_offline_mode` from huggingface_hub. We pin the
# overlap (numpy 2.2.6, pandas 2.3.3, pyarrow 23.0.1, huggingface-hub 1.13.0)
# to the base image's versions so they reinstall as no-ops.
datasets==4.4.2
huggingface-hub==1.13.0
numpy==2.2.6
pandas==2.3.3
pyarrow==23.0.1
