mcp==1.11.0
asyncio==3.4.3
# Match the base image so head and replicas agree (mcp allows pydantic>=2.8,<3); an older head-only pydantic is a cross-node skew.
pydantic==2.12.3
# mcp needs uvicorn>=0.23.1, which rejects the image's 0.22.0 — leaving the seed no
# preference at all, so it re-floats to newest on every recompile. Pin what we tested.
uvicorn==0.52.3
