# langchain <1.1 caps langgraph at <1.1.0, whose loose prebuilt bound lets
# langgraph-prebuilt float to a build importing langgraph.runtime.ExecutionInfo
# (absent from langgraph-core 1.0.x) -> `create_agent` ImportError at replica
# startup (DEPLOY_FAILED). langgraph 1.2.x pins its sub-packages tightly; keep
# this stack aligned with langchain-agent-ray-serve.
langchain==1.3.1
langchain-mcp-adapters==0.1.12
langchain-openai==1.0.2
langgraph==1.2.0
uvicorn==0.38.0
a2a-sdk[http-server]==0.3.22
httpx==0.28.1
mcp==1.22.0
# a2a-sdk requires protobuf>=5.29.5, so the image's 4.25.8 can't hold. Cap it at
# 6.x: protobuf 7 removes FieldDescriptor.label and breaks Ray Serve's
# _proto_to_dict.
protobuf==6.33.6

# Web search + fetch helpers
protego==0.5.0
readabilipy==0.3.0
markdownify==0.14.1
pytest==9.0.2
