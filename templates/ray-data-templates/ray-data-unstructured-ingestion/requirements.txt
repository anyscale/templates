# Core Ray dependencies
ray[data]>=2.8.0

# Data processing and serialization
pyarrow>=12.0.0
pandas>=2.0.0

# Ray Data LLM package and vLLM
# Note: vLLM 0.7.2 is the tested version for Ray Data LLM
vllm==0.7.2

# Document processing (optional - for production use)
# unstructured>=0.10.0
# PyPDF2>=3.0.0
# python-docx>=0.8.11
# openpyxl>=3.1.0

# LLM and ML dependencies
torch>=2.0.0
transformers>=4.30.0
sentence-transformers>=2.2.0

# AWS S3 access
boto3>=1.26.0

# Logging and utilities
python-dateutil>=2.8.0

# For Iceberg support (when available)
# pyiceberg>=0.5.0

# Development and testing
pytest>=7.0.0
black>=23.0.0
flake8>=6.0.0
