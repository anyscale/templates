matplotlib==3.10.6
torch==2.8.0
torchvision==0.23.0
# Ray Data enforces a floor on pyarrow; this is the image's own version, so it clears it.
pyarrow==19.0.1
torchmetrics==1.8.2
xgboost==3.0.5
seaborn==0.13.2
scikit-learn==1.7.2
lightning==2.5.5
datasets==2.19.2
pandas==2.3.3
tqdm==4.67.1
# gymnasium ships in the image at this version; the lock installs --no-deps over it.
gymnasium==1.2.2
