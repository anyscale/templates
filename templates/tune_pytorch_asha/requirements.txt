torch==2.8.0
torchvision==0.23.0
pandas==2.3.3  # image version; nothing else requires it, so unlisted it drops out of the lock
matplotlib==3.10.8  # image version; nothing else requires it, so unlisted it drops out of the lock
